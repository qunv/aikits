import {
  ObservableMap,
  ObservableSet,
  action,
  isAction,
  isComputedProp,
  makeObservable,
  observable,
  reaction,
  runInAction,
  toJS
} from "./chunk-M5ZCAQQA.js";
import "./chunk-RDKGUBC5.js";

// node_modules/mobx-persist-store/lib/esm5/PersistStoreMap.js
var PersistStoreMap = /* @__PURE__ */ new Map();

// node_modules/mobx-persist-store/lib/esm5/clearPersistedStore.js
var __awaiter = function(thisArg, _arguments, P, generator) {
  function adopt(value) {
    return value instanceof P ? value : new P(function(resolve) {
      resolve(value);
    });
  }
  return new (P || (P = Promise))(function(resolve, reject) {
    function fulfilled(value) {
      try {
        step(generator.next(value));
      } catch (e) {
        reject(e);
      }
    }
    function rejected(value) {
      try {
        step(generator["throw"](value));
      } catch (e) {
        reject(e);
      }
    }
    function step(result) {
      result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected);
    }
    step((generator = generator.apply(thisArg, _arguments || [])).next());
  });
};
var __generator = function(thisArg, body) {
  var _ = { label: 0, sent: function() {
    if (t[0] & 1) throw t[1];
    return t[1];
  }, trys: [], ops: [] }, f, y, t, g;
  return g = { next: verb(0), "throw": verb(1), "return": verb(2) }, typeof Symbol === "function" && (g[Symbol.iterator] = function() {
    return this;
  }), g;
  function verb(n) {
    return function(v) {
      return step([n, v]);
    };
  }
  function step(op) {
    if (f) throw new TypeError("Generator is already executing.");
    while (g && (g = 0, op[0] && (_ = 0)), _) try {
      if (f = 1, y && (t = op[0] & 2 ? y["return"] : op[0] ? y["throw"] || ((t = y["return"]) && t.call(y), 0) : y.next) && !(t = t.call(y, op[1])).done) return t;
      if (y = 0, t) op = [op[0] & 2, t.value];
      switch (op[0]) {
        case 0:
        case 1:
          t = op;
          break;
        case 4:
          _.label++;
          return { value: op[1], done: false };
        case 5:
          _.label++;
          y = op[1];
          op = [0];
          continue;
        case 7:
          op = _.ops.pop();
          _.trys.pop();
          continue;
        default:
          if (!(t = _.trys, t = t.length > 0 && t[t.length - 1]) && (op[0] === 6 || op[0] === 2)) {
            _ = 0;
            continue;
          }
          if (op[0] === 3 && (!t || op[1] > t[0] && op[1] < t[3])) {
            _.label = op[1];
            break;
          }
          if (op[0] === 6 && _.label < t[1]) {
            _.label = t[1];
            t = op;
            break;
          }
          if (t && _.label < t[2]) {
            _.label = t[2];
            _.ops.push(op);
            break;
          }
          if (t[2]) _.ops.pop();
          _.trys.pop();
          continue;
      }
      op = body.call(thisArg, _);
    } catch (e) {
      op = [6, e];
      y = 0;
    } finally {
      f = t = 0;
    }
    if (op[0] & 5) throw op[1];
    return { value: op[0] ? op[1] : void 0, done: true };
  }
};
var clearPersistedStore = function(target) {
  return __awaiter(void 0, void 0, void 0, function() {
    var _a;
    return __generator(this, function(_b) {
      switch (_b.label) {
        case 0:
          return [4, (_a = PersistStoreMap.get(target)) === null || _a === void 0 ? void 0 : _a.clearPersistedStore()];
        case 1:
          _b.sent();
          return [
            2
            /*return*/
          ];
      }
    });
  });
};

// node_modules/mobx-persist-store/lib/esm5/configurePersistable.js
var mpsConfig = {};
var mpsReactionOptions = {};
var configurePersistable = function(config, reactionOptions) {
  if (reactionOptions === void 0) {
    reactionOptions = {};
  }
  mpsConfig = config;
  mpsReactionOptions = reactionOptions;
};

// node_modules/mobx-persist-store/lib/esm5/getPersistedStore.js
var __awaiter2 = function(thisArg, _arguments, P, generator) {
  function adopt(value) {
    return value instanceof P ? value : new P(function(resolve) {
      resolve(value);
    });
  }
  return new (P || (P = Promise))(function(resolve, reject) {
    function fulfilled(value) {
      try {
        step(generator.next(value));
      } catch (e) {
        reject(e);
      }
    }
    function rejected(value) {
      try {
        step(generator["throw"](value));
      } catch (e) {
        reject(e);
      }
    }
    function step(result) {
      result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected);
    }
    step((generator = generator.apply(thisArg, _arguments || [])).next());
  });
};
var __generator2 = function(thisArg, body) {
  var _ = { label: 0, sent: function() {
    if (t[0] & 1) throw t[1];
    return t[1];
  }, trys: [], ops: [] }, f, y, t, g;
  return g = { next: verb(0), "throw": verb(1), "return": verb(2) }, typeof Symbol === "function" && (g[Symbol.iterator] = function() {
    return this;
  }), g;
  function verb(n) {
    return function(v) {
      return step([n, v]);
    };
  }
  function step(op) {
    if (f) throw new TypeError("Generator is already executing.");
    while (g && (g = 0, op[0] && (_ = 0)), _) try {
      if (f = 1, y && (t = op[0] & 2 ? y["return"] : op[0] ? y["throw"] || ((t = y["return"]) && t.call(y), 0) : y.next) && !(t = t.call(y, op[1])).done) return t;
      if (y = 0, t) op = [op[0] & 2, t.value];
      switch (op[0]) {
        case 0:
        case 1:
          t = op;
          break;
        case 4:
          _.label++;
          return { value: op[1], done: false };
        case 5:
          _.label++;
          y = op[1];
          op = [0];
          continue;
        case 7:
          op = _.ops.pop();
          _.trys.pop();
          continue;
        default:
          if (!(t = _.trys, t = t.length > 0 && t[t.length - 1]) && (op[0] === 6 || op[0] === 2)) {
            _ = 0;
            continue;
          }
          if (op[0] === 3 && (!t || op[1] > t[0] && op[1] < t[3])) {
            _.label = op[1];
            break;
          }
          if (op[0] === 6 && _.label < t[1]) {
            _.label = t[1];
            t = op;
            break;
          }
          if (t && _.label < t[2]) {
            _.label = t[2];
            _.ops.push(op);
            break;
          }
          if (t[2]) _.ops.pop();
          _.trys.pop();
          continue;
      }
      op = body.call(thisArg, _);
    } catch (e) {
      op = [6, e];
      y = 0;
    } finally {
      f = t = 0;
    }
    if (op[0] & 5) throw op[1];
    return { value: op[0] ? op[1] : void 0, done: true };
  }
};
var getPersistedStore = function(target) {
  return __awaiter2(void 0, void 0, void 0, function() {
    var _a, _b;
    return __generator2(this, function(_c) {
      return [2, (_b = (_a = PersistStoreMap.get(target)) === null || _a === void 0 ? void 0 : _a.getPersistedStore()) !== null && _b !== void 0 ? _b : null];
    });
  });
};

// node_modules/mobx-persist-store/lib/esm5/hydrateStore.js
var __awaiter3 = function(thisArg, _arguments, P, generator) {
  function adopt(value) {
    return value instanceof P ? value : new P(function(resolve) {
      resolve(value);
    });
  }
  return new (P || (P = Promise))(function(resolve, reject) {
    function fulfilled(value) {
      try {
        step(generator.next(value));
      } catch (e) {
        reject(e);
      }
    }
    function rejected(value) {
      try {
        step(generator["throw"](value));
      } catch (e) {
        reject(e);
      }
    }
    function step(result) {
      result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected);
    }
    step((generator = generator.apply(thisArg, _arguments || [])).next());
  });
};
var __generator3 = function(thisArg, body) {
  var _ = { label: 0, sent: function() {
    if (t[0] & 1) throw t[1];
    return t[1];
  }, trys: [], ops: [] }, f, y, t, g;
  return g = { next: verb(0), "throw": verb(1), "return": verb(2) }, typeof Symbol === "function" && (g[Symbol.iterator] = function() {
    return this;
  }), g;
  function verb(n) {
    return function(v) {
      return step([n, v]);
    };
  }
  function step(op) {
    if (f) throw new TypeError("Generator is already executing.");
    while (g && (g = 0, op[0] && (_ = 0)), _) try {
      if (f = 1, y && (t = op[0] & 2 ? y["return"] : op[0] ? y["throw"] || ((t = y["return"]) && t.call(y), 0) : y.next) && !(t = t.call(y, op[1])).done) return t;
      if (y = 0, t) op = [op[0] & 2, t.value];
      switch (op[0]) {
        case 0:
        case 1:
          t = op;
          break;
        case 4:
          _.label++;
          return { value: op[1], done: false };
        case 5:
          _.label++;
          y = op[1];
          op = [0];
          continue;
        case 7:
          op = _.ops.pop();
          _.trys.pop();
          continue;
        default:
          if (!(t = _.trys, t = t.length > 0 && t[t.length - 1]) && (op[0] === 6 || op[0] === 2)) {
            _ = 0;
            continue;
          }
          if (op[0] === 3 && (!t || op[1] > t[0] && op[1] < t[3])) {
            _.label = op[1];
            break;
          }
          if (op[0] === 6 && _.label < t[1]) {
            _.label = t[1];
            t = op;
            break;
          }
          if (t && _.label < t[2]) {
            _.label = t[2];
            _.ops.push(op);
            break;
          }
          if (t[2]) _.ops.pop();
          _.trys.pop();
          continue;
      }
      op = body.call(thisArg, _);
    } catch (e) {
      op = [6, e];
      y = 0;
    } finally {
      f = t = 0;
    }
    if (op[0] & 5) throw op[1];
    return { value: op[0] ? op[1] : void 0, done: true };
  }
};
var hydrateStore = function(target) {
  return __awaiter3(void 0, void 0, void 0, function() {
    var _a;
    return __generator3(this, function(_b) {
      switch (_b.label) {
        case 0:
          return [4, (_a = PersistStoreMap.get(target)) === null || _a === void 0 ? void 0 : _a.hydrateStore()];
        case 1:
          _b.sent();
          return [
            2
            /*return*/
          ];
      }
    });
  });
};

// node_modules/mobx-persist-store/lib/esm5/isHydrated.js
var isHydrated = function(target) {
  var _a, _b;
  return (_b = (_a = PersistStoreMap.get(target)) === null || _a === void 0 ? void 0 : _a.isHydrated) !== null && _b !== void 0 ? _b : false;
};

// node_modules/mobx-persist-store/lib/esm5/isPersisting.js
var isPersisting = function(target) {
  var _a, _b;
  return (_b = (_a = PersistStoreMap.get(target)) === null || _a === void 0 ? void 0 : _a.isPersisting) !== null && _b !== void 0 ? _b : false;
};

// node_modules/mobx-persist-store/lib/esm5/utils.js
var buildExpireTimestamp = function(milliseconds) {
  return (/* @__PURE__ */ new Date()).getTime() + milliseconds;
};
var hasTimestampExpired = function(milliseconds) {
  var dateTimeNow = (/* @__PURE__ */ new Date()).getTime();
  var dateTimeExpiration = new Date(milliseconds).getTime();
  return dateTimeExpiration <= dateTimeNow;
};
var isObject = function(data) {
  return Boolean(data) && Array.isArray(data) === false && typeof data === "object";
};
var isFunction = function(functionToCheck) {
  return functionToCheck && typeof functionToCheck === "function";
};
var isStorageControllerLike = function(value) {
  if (typeof Storage !== "undefined" && value instanceof Storage) {
    return true;
  }
  return [
    value === null || value === void 0 ? void 0 : value.hasOwnProperty("getItem"),
    value === null || value === void 0 ? void 0 : value.hasOwnProperty("removeItem"),
    value === null || value === void 0 ? void 0 : value.hasOwnProperty("setItem"),
    isFunction(value === null || value === void 0 ? void 0 : value.getItem),
    isFunction(value === null || value === void 0 ? void 0 : value.removeItem),
    isFunction(value === null || value === void 0 ? void 0 : value.setItem)
  ].every(Boolean);
};
var isBrowser = typeof window !== "undefined";
var isNotProductionBuild = true;
var invalidStorageAdaptorWarningIf = function(storageAdaptor, storageName) {
  if (isBrowser && isNotProductionBuild && !isStorageControllerLike(storageAdaptor)) {
    console.warn("mobx-persist-store: ".concat(storageName, ` does not have a valid storage adaptor.

* Make sure the storage controller has 'getItem', 'setItem' and 'removeItem' methods."`));
  }
};
var duplicatedStoreWarningIf = function(hasPersistedStoreAlready, storageName) {
  if (isBrowser && isNotProductionBuild && hasPersistedStoreAlready) {
    console.warn(`mobx-persist-store: 'makePersistable' was called with the same storage name "`.concat(storageName, '".\n\n * Make sure you call "stopPersisting" before recreating "').concat(storageName, '" to avoid memory leaks. \n * Or double check you did not have two stores with the same name.'));
  }
};
var computedPersistWarningIf = function(isComputedProperty, propertyName) {
  if (isBrowser && isNotProductionBuild && isComputedProperty) {
    console.warn("mobx-persist-store: The property '".concat(propertyName, "' is computed and will not persist."));
  }
};
var actionPersistWarningIf = function(isComputedProperty, propertyName) {
  if (isBrowser && isNotProductionBuild && isComputedProperty) {
    console.warn("mobx-persist-store: The property '".concat(propertyName, "' is an action and will not persist."));
  }
};
var consoleDebug = function(isDebugMode, message, content) {
  if (content === void 0) {
    content = "";
  }
  if (isDebugMode && isBrowser && isNotProductionBuild) {
    console.info("%c mobx-persist-store: (Debug Mode) ".concat(message, " "), "background: #4B8CC5; color: black; display: block;", content);
  }
};
var isArrayForMap = function(value) {
  if (Array.isArray(value)) {
    return value.every(function(v) {
      return Array.isArray(v);
    });
  }
  return false;
};
var isArrayForSet = function(value) {
  return Array.isArray(value);
};
var omitObjectProperties = function(obj, testFn) {
  Object.keys(obj).forEach(function(key) {
    return testFn(obj[key]) && delete obj[key];
  });
  return obj;
};

// node_modules/mobx-persist-store/lib/esm5/StorageAdapter.js
var __awaiter4 = function(thisArg, _arguments, P, generator) {
  function adopt(value) {
    return value instanceof P ? value : new P(function(resolve) {
      resolve(value);
    });
  }
  return new (P || (P = Promise))(function(resolve, reject) {
    function fulfilled(value) {
      try {
        step(generator.next(value));
      } catch (e) {
        reject(e);
      }
    }
    function rejected(value) {
      try {
        step(generator["throw"](value));
      } catch (e) {
        reject(e);
      }
    }
    function step(result) {
      result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected);
    }
    step((generator = generator.apply(thisArg, _arguments || [])).next());
  });
};
var __generator4 = function(thisArg, body) {
  var _ = { label: 0, sent: function() {
    if (t[0] & 1) throw t[1];
    return t[1];
  }, trys: [], ops: [] }, f, y, t, g;
  return g = { next: verb(0), "throw": verb(1), "return": verb(2) }, typeof Symbol === "function" && (g[Symbol.iterator] = function() {
    return this;
  }), g;
  function verb(n) {
    return function(v) {
      return step([n, v]);
    };
  }
  function step(op) {
    if (f) throw new TypeError("Generator is already executing.");
    while (g && (g = 0, op[0] && (_ = 0)), _) try {
      if (f = 1, y && (t = op[0] & 2 ? y["return"] : op[0] ? y["throw"] || ((t = y["return"]) && t.call(y), 0) : y.next) && !(t = t.call(y, op[1])).done) return t;
      if (y = 0, t) op = [op[0] & 2, t.value];
      switch (op[0]) {
        case 0:
        case 1:
          t = op;
          break;
        case 4:
          _.label++;
          return { value: op[1], done: false };
        case 5:
          _.label++;
          y = op[1];
          op = [0];
          continue;
        case 7:
          op = _.ops.pop();
          _.trys.pop();
          continue;
        default:
          if (!(t = _.trys, t = t.length > 0 && t[t.length - 1]) && (op[0] === 6 || op[0] === 2)) {
            _ = 0;
            continue;
          }
          if (op[0] === 3 && (!t || op[1] > t[0] && op[1] < t[3])) {
            _.label = op[1];
            break;
          }
          if (op[0] === 6 && _.label < t[1]) {
            _.label = t[1];
            t = op;
            break;
          }
          if (t && _.label < t[2]) {
            _.label = t[2];
            _.ops.push(op);
            break;
          }
          if (t[2]) _.ops.pop();
          _.trys.pop();
          continue;
      }
      op = body.call(thisArg, _);
    } catch (e) {
      op = [6, e];
      y = 0;
    } finally {
      f = t = 0;
    }
    if (op[0] & 5) throw op[1];
    return { value: op[0] ? op[1] : void 0, done: true };
  }
};
var StorageAdapter = (
  /** @class */
  function() {
    function StorageAdapter2(options) {
      this.options = options;
    }
    StorageAdapter2.prototype.setItem = function(key, item) {
      var _a;
      return __awaiter4(this, void 0, void 0, function() {
        var _b, _c, stringify, _d, debugMode, __mps__, data, content;
        return __generator4(this, function(_e) {
          switch (_e.label) {
            case 0:
              _b = this.options, _c = _b.stringify, stringify = _c === void 0 ? true : _c, _d = _b.debugMode, debugMode = _d === void 0 ? false : _d;
              __mps__ = omitObjectProperties({
                expireInTimestamp: this.options.expireIn ? buildExpireTimestamp(this.options.expireIn) : void 0,
                version: this.options.version
              }, function(value) {
                return value === void 0;
              });
              data = Object.keys(__mps__).length ? Object.assign({}, item, {
                __mps__
              }) : item;
              content = stringify ? JSON.stringify(data) : data;
              consoleDebug(debugMode, "".concat(key, " - setItem:"), content);
              return [4, (_a = this.options.storage) === null || _a === void 0 ? void 0 : _a.setItem(key, content)];
            case 1:
              _e.sent();
              return [
                2
                /*return*/
              ];
          }
        });
      });
    };
    StorageAdapter2.prototype.getItem = function(key) {
      var _a, _b, _c;
      return __awaiter4(this, void 0, void 0, function() {
        var _d, _e, removeOnExpiration, _f, debugMode, version, storageData, parsedData, hasExpired, mismatchedVersion;
        return __generator4(this, function(_g) {
          switch (_g.label) {
            case 0:
              _d = this.options, _e = _d.removeOnExpiration, removeOnExpiration = _e === void 0 ? true : _e, _f = _d.debugMode, debugMode = _f === void 0 ? false : _f, version = _d.version;
              return [4, (_a = this.options.storage) === null || _a === void 0 ? void 0 : _a.getItem(key)];
            case 1:
              storageData = _g.sent();
              try {
                parsedData = JSON.parse(storageData) || {};
              } catch (error) {
                parsedData = storageData || {};
              }
              hasExpired = hasTimestampExpired((_b = parsedData.__mps__) === null || _b === void 0 ? void 0 : _b.expireInTimestamp);
              mismatchedVersion = version && ((_c = parsedData.__mps__) === null || _c === void 0 ? void 0 : _c.version) !== version;
              consoleDebug(debugMode, "".concat(key, " - hasExpired"), hasExpired);
              consoleDebug(debugMode, "".concat(key, " - mismatchedVersion"), mismatchedVersion);
              if (!(hasExpired && removeOnExpiration || mismatchedVersion)) return [3, 3];
              return [4, this.removeItem(key)];
            case 2:
              _g.sent();
              _g.label = 3;
            case 3:
              parsedData = hasExpired || mismatchedVersion ? {} : parsedData;
              consoleDebug(debugMode, "".concat(key, " - (getItem):"), parsedData);
              return [2, parsedData];
          }
        });
      });
    };
    StorageAdapter2.prototype.removeItem = function(key) {
      var _a;
      return __awaiter4(this, void 0, void 0, function() {
        var _b, debugMode;
        return __generator4(this, function(_c) {
          switch (_c.label) {
            case 0:
              _b = this.options.debugMode, debugMode = _b === void 0 ? false : _b;
              consoleDebug(debugMode, "".concat(key, " - (removeItem): storage was removed"));
              return [4, (_a = this.options.storage) === null || _a === void 0 ? void 0 : _a.removeItem(key)];
            case 1:
              _c.sent();
              return [
                2
                /*return*/
              ];
          }
        });
      });
    };
    return StorageAdapter2;
  }()
);

// node_modules/mobx-persist-store/lib/esm5/serializableProperty.js
var isSerializableProperty = function(obj) {
  var keys = ["key", "serialize", "deserialize"];
  if (!isObject(obj)) {
    consoleDebug(!!mpsConfig.debugMode, "passed value is not an object", { obj });
    return false;
  }
  return keys.every(function(key) {
    if (obj.hasOwnProperty(key) && typeof key !== "undefined") {
      return true;
    }
    consoleDebug(!!mpsConfig.debugMode, "".concat(String(key), " not found in SerializableProperty"), { key, obj });
    return false;
  });
};
var makeSerializableProperties = function(properties) {
  return properties.reduce(function(acc, curr) {
    if (typeof curr === "string") {
      acc.push({
        key: curr,
        serialize: function(value) {
          return value;
        },
        deserialize: function(value) {
          return value;
        }
      });
      return acc;
    }
    if (isSerializableProperty(curr)) {
      acc.push(curr);
      return acc;
    }
    return acc;
  }, []);
};

// node_modules/mobx-persist-store/lib/esm5/PersistStore.js
var __awaiter5 = function(thisArg, _arguments, P, generator) {
  function adopt(value) {
    return value instanceof P ? value : new P(function(resolve) {
      resolve(value);
    });
  }
  return new (P || (P = Promise))(function(resolve, reject) {
    function fulfilled(value) {
      try {
        step(generator.next(value));
      } catch (e) {
        reject(e);
      }
    }
    function rejected(value) {
      try {
        step(generator["throw"](value));
      } catch (e) {
        reject(e);
      }
    }
    function step(result) {
      result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected);
    }
    step((generator = generator.apply(thisArg, _arguments || [])).next());
  });
};
var __generator5 = function(thisArg, body) {
  var _ = { label: 0, sent: function() {
    if (t[0] & 1) throw t[1];
    return t[1];
  }, trys: [], ops: [] }, f, y, t, g;
  return g = { next: verb(0), "throw": verb(1), "return": verb(2) }, typeof Symbol === "function" && (g[Symbol.iterator] = function() {
    return this;
  }), g;
  function verb(n) {
    return function(v) {
      return step([n, v]);
    };
  }
  function step(op) {
    if (f) throw new TypeError("Generator is already executing.");
    while (g && (g = 0, op[0] && (_ = 0)), _) try {
      if (f = 1, y && (t = op[0] & 2 ? y["return"] : op[0] ? y["throw"] || ((t = y["return"]) && t.call(y), 0) : y.next) && !(t = t.call(y, op[1])).done) return t;
      if (y = 0, t) op = [op[0] & 2, t.value];
      switch (op[0]) {
        case 0:
        case 1:
          t = op;
          break;
        case 4:
          _.label++;
          return { value: op[1], done: false };
        case 5:
          _.label++;
          y = op[1];
          op = [0];
          continue;
        case 7:
          op = _.ops.pop();
          _.trys.pop();
          continue;
        default:
          if (!(t = _.trys, t = t.length > 0 && t[t.length - 1]) && (op[0] === 6 || op[0] === 2)) {
            _ = 0;
            continue;
          }
          if (op[0] === 3 && (!t || op[1] > t[0] && op[1] < t[3])) {
            _.label = op[1];
            break;
          }
          if (op[0] === 6 && _.label < t[1]) {
            _.label = t[1];
            t = op;
            break;
          }
          if (t && _.label < t[2]) {
            _.label = t[2];
            _.ops.push(op);
            break;
          }
          if (t[2]) _.ops.pop();
          _.trys.pop();
          continue;
      }
      op = body.call(thisArg, _);
    } catch (e) {
      op = [6, e];
      y = 0;
    } finally {
      f = t = 0;
    }
    if (op[0] & 5) throw op[1];
    return { value: op[0] ? op[1] : void 0, done: true };
  }
};
var PersistStore = (
  /** @class */
  function() {
    function PersistStore2(target, options, reactionOptions) {
      if (reactionOptions === void 0) {
        reactionOptions = {};
      }
      var _a, _b, _c, _d, _e, _f, _g, _h;
      this.cancelWatch = null;
      this.properties = [];
      this.reactionOptions = {};
      this.storageAdapter = null;
      this.target = null;
      this.version = void 0;
      this.debugMode = false;
      this.isHydrated = false;
      this.isPersisting = false;
      this.storageName = "";
      this.target = target;
      this.storageName = options.name;
      this.version = (_a = options.version) !== null && _a !== void 0 ? _a : mpsConfig.version;
      this.properties = makeSerializableProperties(options.properties);
      this.reactionOptions = Object.assign({ fireImmediately: true }, mpsReactionOptions, reactionOptions);
      this.debugMode = (_c = (_b = options.debugMode) !== null && _b !== void 0 ? _b : mpsConfig.debugMode) !== null && _c !== void 0 ? _c : false;
      this.storageAdapter = new StorageAdapter({
        version: this.version,
        expireIn: (_d = options.expireIn) !== null && _d !== void 0 ? _d : mpsConfig.expireIn,
        removeOnExpiration: (_f = (_e = options.removeOnExpiration) !== null && _e !== void 0 ? _e : mpsConfig.removeOnExpiration) !== null && _f !== void 0 ? _f : true,
        stringify: (_h = (_g = options.stringify) !== null && _g !== void 0 ? _g : mpsConfig.stringify) !== null && _h !== void 0 ? _h : true,
        storage: options.storage ? options.storage : mpsConfig.storage,
        debugMode: this.debugMode
      });
      makeObservable(this, {
        clearPersistedStore: action,
        hydrateStore: action,
        isHydrated: observable,
        isPersisting: observable,
        pausePersisting: action,
        startPersisting: action,
        stopPersisting: action
      }, { autoBind: true, deep: false });
      invalidStorageAdaptorWarningIf(this.storageAdapter.options.storage, this.storageName);
      consoleDebug(this.debugMode, "".concat(this.storageName, " - (makePersistable)"), {
        properties: this.properties,
        storageAdapter: this.storageAdapter,
        reactionOptions: this.reactionOptions
      });
    }
    PersistStore2.prototype.init = function() {
      return __awaiter5(this, void 0, void 0, function() {
        return __generator5(this, function(_a) {
          switch (_a.label) {
            case 0:
              return [4, this.hydrateStore()];
            case 1:
              _a.sent();
              this.startPersisting();
              return [2, this];
          }
        });
      });
    };
    PersistStore2.prototype.hydrateStore = function() {
      return __awaiter5(this, void 0, void 0, function() {
        var isBeingWatched, data_1, target_1;
        var _this = this;
        return __generator5(this, function(_a) {
          switch (_a.label) {
            case 0:
              isBeingWatched = Boolean(this.cancelWatch);
              if (this.isPersisting) {
                this.pausePersisting();
              }
              runInAction(function() {
                _this.isHydrated = false;
                consoleDebug(_this.debugMode, "".concat(_this.storageName, " - (hydrateStore) isHydrated:"), _this.isHydrated);
              });
              if (!(this.storageAdapter && this.target)) return [3, 2];
              return [4, this.storageAdapter.getItem(this.storageName)];
            case 1:
              data_1 = _a.sent();
              target_1 = this.target;
              if (data_1) {
                runInAction(function() {
                  _this.properties.forEach(function(property) {
                    var allowPropertyHydration = [
                      property.key in target_1,
                      typeof data_1[property.key] !== "undefined"
                    ].every(Boolean);
                    if (allowPropertyHydration) {
                      var propertyData = data_1[property.key];
                      if (target_1[property.key] instanceof ObservableMap && isArrayForMap(propertyData)) {
                        target_1[property.key] = property.deserialize(new Map(propertyData));
                      } else if (target_1[property.key] instanceof ObservableSet && isArrayForSet(propertyData)) {
                        target_1[property.key] = property.deserialize(new Set(propertyData));
                      } else {
                        target_1[property.key] = property.deserialize(propertyData);
                      }
                    }
                  });
                });
              }
              _a.label = 2;
            case 2:
              runInAction(function() {
                _this.isHydrated = true;
                consoleDebug(_this.debugMode, "".concat(_this.storageName, " - isHydrated:"), _this.isHydrated);
              });
              if (isBeingWatched) {
                this.startPersisting();
              }
              return [
                2
                /*return*/
              ];
          }
        });
      });
    };
    PersistStore2.prototype.startPersisting = function() {
      var _this = this;
      if (!this.storageAdapter || !this.target || this.cancelWatch) {
        return;
      }
      var target = this.target;
      this.cancelWatch = reaction(function() {
        var propertiesToWatch = {};
        _this.properties.forEach(function(property) {
          var isComputedProperty = isComputedProp(target, property.key);
          var isActionProperty = isAction(target[property.key]);
          computedPersistWarningIf(isComputedProperty, String(property.key));
          actionPersistWarningIf(isActionProperty, String(property.key));
          if (!isComputedProperty && !isActionProperty) {
            var propertyData = property.serialize(target[property.key]);
            if (propertyData instanceof ObservableMap) {
              var mapArray_1 = [];
              propertyData.forEach(function(v, k) {
                mapArray_1.push([k, toJS(v)]);
              });
              propertyData = mapArray_1;
            } else if (propertyData instanceof ObservableSet) {
              propertyData = Array.from(propertyData).map(toJS);
            }
            propertiesToWatch[property.key] = toJS(propertyData);
          }
        });
        return propertiesToWatch;
      }, function(dataToSave) {
        return __awaiter5(_this, void 0, void 0, function() {
          return __generator5(this, function(_a) {
            switch (_a.label) {
              case 0:
                if (!this.storageAdapter) return [3, 2];
                return [4, this.storageAdapter.setItem(this.storageName, dataToSave)];
              case 1:
                _a.sent();
                _a.label = 2;
              case 2:
                return [
                  2
                  /*return*/
                ];
            }
          });
        });
      }, this.reactionOptions);
      this.isPersisting = true;
      consoleDebug(this.debugMode, "".concat(this.storageName, " - (startPersisting) isPersisting:"), this.isPersisting);
    };
    PersistStore2.prototype.pausePersisting = function() {
      this.isPersisting = false;
      consoleDebug(this.debugMode, "".concat(this.storageName, " - pausePersisting (isPersisting):"), this.isPersisting);
      if (this.cancelWatch) {
        this.cancelWatch();
        this.cancelWatch = null;
      }
    };
    PersistStore2.prototype.stopPersisting = function() {
      this.pausePersisting();
      consoleDebug(this.debugMode, "".concat(this.storageName, " - (stopPersisting)"));
      PersistStoreMap.delete(this.target);
      this.cancelWatch = null;
      this.properties = [];
      this.reactionOptions = {};
      this.storageAdapter = null;
      this.target = null;
    };
    PersistStore2.prototype.clearPersistedStore = function() {
      return __awaiter5(this, void 0, void 0, function() {
        return __generator5(this, function(_a) {
          switch (_a.label) {
            case 0:
              if (!this.storageAdapter) return [3, 2];
              consoleDebug(this.debugMode, "".concat(this.storageName, " - (clearPersistedStore)"));
              return [4, this.storageAdapter.removeItem(this.storageName)];
            case 1:
              _a.sent();
              _a.label = 2;
            case 2:
              return [
                2
                /*return*/
              ];
          }
        });
      });
    };
    PersistStore2.prototype.getPersistedStore = function() {
      return __awaiter5(this, void 0, void 0, function() {
        return __generator5(this, function(_a) {
          if (this.storageAdapter) {
            consoleDebug(this.debugMode, "".concat(this.storageName, " - (getPersistedStore)"));
            return [2, this.storageAdapter.getItem(this.storageName)];
          }
          return [2, null];
        });
      });
    };
    return PersistStore2;
  }()
);

// node_modules/mobx-persist-store/lib/esm5/makePersistable.js
var __awaiter6 = function(thisArg, _arguments, P, generator) {
  function adopt(value) {
    return value instanceof P ? value : new P(function(resolve) {
      resolve(value);
    });
  }
  return new (P || (P = Promise))(function(resolve, reject) {
    function fulfilled(value) {
      try {
        step(generator.next(value));
      } catch (e) {
        reject(e);
      }
    }
    function rejected(value) {
      try {
        step(generator["throw"](value));
      } catch (e) {
        reject(e);
      }
    }
    function step(result) {
      result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected);
    }
    step((generator = generator.apply(thisArg, _arguments || [])).next());
  });
};
var __generator6 = function(thisArg, body) {
  var _ = { label: 0, sent: function() {
    if (t[0] & 1) throw t[1];
    return t[1];
  }, trys: [], ops: [] }, f, y, t, g;
  return g = { next: verb(0), "throw": verb(1), "return": verb(2) }, typeof Symbol === "function" && (g[Symbol.iterator] = function() {
    return this;
  }), g;
  function verb(n) {
    return function(v) {
      return step([n, v]);
    };
  }
  function step(op) {
    if (f) throw new TypeError("Generator is already executing.");
    while (g && (g = 0, op[0] && (_ = 0)), _) try {
      if (f = 1, y && (t = op[0] & 2 ? y["return"] : op[0] ? y["throw"] || ((t = y["return"]) && t.call(y), 0) : y.next) && !(t = t.call(y, op[1])).done) return t;
      if (y = 0, t) op = [op[0] & 2, t.value];
      switch (op[0]) {
        case 0:
        case 1:
          t = op;
          break;
        case 4:
          _.label++;
          return { value: op[1], done: false };
        case 5:
          _.label++;
          y = op[1];
          op = [0];
          continue;
        case 7:
          op = _.ops.pop();
          _.trys.pop();
          continue;
        default:
          if (!(t = _.trys, t = t.length > 0 && t[t.length - 1]) && (op[0] === 6 || op[0] === 2)) {
            _ = 0;
            continue;
          }
          if (op[0] === 3 && (!t || op[1] > t[0] && op[1] < t[3])) {
            _.label = op[1];
            break;
          }
          if (op[0] === 6 && _.label < t[1]) {
            _.label = t[1];
            t = op;
            break;
          }
          if (t && _.label < t[2]) {
            _.label = t[2];
            _.ops.push(op);
            break;
          }
          if (t[2]) _.ops.pop();
          _.trys.pop();
          continue;
      }
      op = body.call(thisArg, _);
    } catch (e) {
      op = [6, e];
      y = 0;
    } finally {
      f = t = 0;
    }
    if (op[0] & 5) throw op[1];
    return { value: op[0] ? op[1] : void 0, done: true };
  }
};
var setMobxPersistStore = function(target, persistStore) {
  if (true) {
    for (var _i = 0, _a = PersistStoreMap.entries(); _i < _a.length; _i++) {
      var _b = _a[_i], key = _b[0], store = _b[1];
      if (store.storageName === persistStore.storageName) {
        store.stopPersisting();
        PersistStoreMap.delete(key);
      }
    }
  }
  PersistStoreMap.set(target, persistStore);
};
var makePersistable = function(target, storageOptions, reactionOptions) {
  return __awaiter6(void 0, void 0, void 0, function() {
    var mobxPersistStore, hasPersistedStoreAlready;
    return __generator6(this, function(_a) {
      mobxPersistStore = new PersistStore(target, storageOptions, reactionOptions);
      hasPersistedStoreAlready = Array.from(PersistStoreMap.values()).map(function(item) {
        return item.storageName;
      }).includes(mobxPersistStore.storageName);
      duplicatedStoreWarningIf(hasPersistedStoreAlready, mobxPersistStore.storageName);
      setMobxPersistStore(target, mobxPersistStore);
      return [2, mobxPersistStore.init()];
    });
  });
};

// node_modules/mobx-persist-store/lib/esm5/pausePersisting.js
var pausePersisting = function(target) {
  var _a;
  (_a = PersistStoreMap.get(target)) === null || _a === void 0 ? void 0 : _a.pausePersisting();
};

// node_modules/mobx-persist-store/lib/esm5/startPersisting.js
var startPersisting = function(target) {
  var _a;
  (_a = PersistStoreMap.get(target)) === null || _a === void 0 ? void 0 : _a.startPersisting();
};

// node_modules/mobx-persist-store/lib/esm5/stopPersisting.js
var stopPersisting = function(target) {
  var _a;
  (_a = PersistStoreMap.get(target)) === null || _a === void 0 ? void 0 : _a.stopPersisting();
};
export {
  PersistStore,
  PersistStoreMap,
  StorageAdapter,
  clearPersistedStore,
  configurePersistable,
  getPersistedStore,
  hydrateStore,
  isHydrated,
  isPersisting,
  makePersistable,
  mpsConfig,
  mpsReactionOptions,
  pausePersisting,
  startPersisting,
  stopPersisting
};
//# sourceMappingURL=mobx-persist-store.js.map
